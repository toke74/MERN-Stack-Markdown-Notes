# 1. Env Variables In Vite

Vite exposes env variables under import.meta.env object as strings automatically.

To prevent accidentally leaking env variables to the client, only variables prefixed with VITE\_ are exposed to your Vite-processed code. e.g. for the following env variables:

.env

```js
VITE_SOME_KEY = 123;
DB_PASSWORD = foobar;
```

Only `VITE_SOME_KEY` will be exposed as `import.meta.env.VITE_SOME_KEY` to your client source code, but `DB_PASSWORD` will not.

```js
console.log(import.meta.env.VITE_SOME_KEY); // "123"
console.log(import.meta.env.DB_PASSWORD); // undefined
```

## Create .env file at route folder (Frontend)

![](/Frontend//image/1.png)

# 2 Multi-Vendor E-commerce Frontend Specifications

## 2.1 🔧 Technologies Used

- React JS – SPA architecture

- Redux Toolkit (RTK) – for global state management

- RTK Query – for data fetching and caching

- Tailwind CSS v4 – for styling

- React Router DOM v6 – for routing

- React Icons – for consistent icons

- Form Libraries – e.g., react-hook-form + zod for forms and validation

- Headless UI or @radix-ui/react-popover – for UI components like modals, menus

## 2.2 🧱 Project Structure

```bash
/src
│
├── /components
│     /nav
│       ├── /Navbar.jsx                # Desktop Navbar
│       ├── /CategoryDropdown.jsx      # Desktop Category Dropdown
│       ├── /MobileMenu.jsx            # Mobile Navigation
│       ├── /MobileBottomNav.jsx       # Mobile Bottom Navigation
│       ├── /CategoryMobileNav.jsx     # Mobile Category Navigation
│       ├── /TopNav.jsx                # Desktop top navigation
├     /banner
│       ├── /Banner.jsx
│
├── /features             # RTK slices & API features (user, product, cart)
├── /hooks                # Custom hooks
├── /layouts              # Page layouts (VendorLayout, UserLayout, AdminLayout)
├── /pages                # Route-level pages
│   ├── /home
│   ├── /auth
│   ├── /user
│   ├── /vendor
│   └── /admin
│   └── /404.jsx
├── /routes               # Route configuration
├── /services             # RTK Query APIs
└── /utils
├      ├── /data.jsx
│      ├── /ProductShowcaseData.js
├── /app.jsx
└── /firebase.js
├── /index.css
└── /main.jsx

```

# 3 Navbar Requirements:

- Links: Home, Categories, Products, Shops, Sellers

- Icons: Cart, Wishlist

- Auth Items: Login/Register or Profile dropdown

- Responsive Design: Mobile menu with hamburger icon

# 4 Navigation Codes

![](/Frontend/image/2.png)

## Top Navigation

` TopNav.jsx`

```js
//React icons
import { FaFacebook, FaTwitter, FaInstagram, FaLinkedin } from "react-icons/fa";

const TopNav = () => {
  return (
    <div className="hidden md:block bg-yellow-200 border-b border-gray-200 py-2">
      <div className="container mx-auto flex justify-between items-center px-3 py-2 h-4">
        <ul className="flex space-x-4 text-xl">
          <li>
            <a
              href="#"
              className="text-secondary text-[18px] hover:text-secondary/85 cursor-pointer "
            >
              <FaFacebook />
            </a>
          </li>
          <li>
            <a
              href="#"
              className="text-secondary text-[18px] hover:text-secondary/85 cursor-pointer"
            >
              <FaTwitter name="logo-twitter" />
            </a>
          </li>
          <li>
            <a
              href="#"
              className="text-secondary text-[18px] hover:text-secondary/85 cursor-pointer"
            >
              <FaInstagram name="logo-instagram" />
            </a>
          </li>
          <li>
            <a
              href="#"
              className="text-secondary text-[18px] hover:text-secondary/85 cursor-pointer"
            >
              <FaLinkedin name="logo-linkedin" />
            </a>
          </li>
        </ul>

        <div className="text-text text-[12px]">
          <p>
            <b className="">Free Shipping</b> This Week Order Over - $55
          </p>
        </div>

        <div className="flex space-x-4">
          <select
            name="currency"
            className="border border-gray-300 rounded-md p-1 text-[9px] cursor-pointer"
          >
            <option value="usd">USD $</option>
            <option value="eur">EUR €</option>
          </select>

          <select
            name="language"
            className="border border-gray-300 rounded-md p-1 text-[9px] cursor-pointer"
          >
            <option value="en-US">English</option>
            <option value="es-ES">Español</option>
            <option value="fr">Français</option>
          </select>
        </div>
      </div>
    </div>
  );
};

export default TopNav;
```

## Main Navigation

`Navbar.jsx`

```js
//Package files
import { Link } from "react-router-dom";

//React icons
import { FaRegHeart } from "react-icons/fa6";
import { IoSearchOutline } from "react-icons/io5";
import { FiShoppingBag } from "react-icons/fi";

//Local files
import CategoryDropdown from "./CategoryDropdown";
import { navigationMenu } from "../../utils/data";

const Navbar = () => {
  return (
    <nav className="bg-white shadow-md fixed w-full top-0 md:top-9 z-30 ">
      <div className="max-w-7xl 2xl:max-w-[1728px] mx-auto h-15 px-4 py-1 flex items-center justify-between">
        {/* Left - Logo and Nav */}
        <div className="flex items-center gap-4">
          {/* Logo */}
          <Link to="/" className="text-xl font-bold text-purple-700">
            <img
              src="/ShopOrbit.svg"
              width={120}
              height={80}
              alt="ShopOrbit"
              className="mx-auto pt-2"
            />
          </Link>
        </div>

        {/* Middle -  Nav */}
        <div className="flex items-center gap-4">
          {/* Desktop Nav */}
          <ul className="hidden lg:flex items-center gap-6 ml-6 text-sm  font-medium text-gray-700">
            <li>
              <CategoryDropdown />
            </li>
            {navigationMenu?.map((nav, index) => (
              <li>
                <Link
                  className="text-sm text-secondary hover:text-primary/85"
                  key={index}
                  to={nav.url}
                >
                  {nav.name}
                </Link>
              </li>
            ))}
          </ul>
        </div>

        {/* Right - Icons & Auth */}
        <div className="flex items-center gap-3 ml-6">
          {/* Search */}
          <Link to="/search" className="relative ">
            <IoSearchOutline className="text-[18px] text-secondary hover:text-primary/85" />
          </Link>

          {/* Wishlist */}
          <Link to="/wishlist" className="relative hidden lg:flex">
            <FaRegHeart className="text-[20px] text-secondary hover:text-primary/85" />
            <span className="absolute -top-1.5 -right-2 bg-red-400 text-white text-[10px] font-medium px-1 py-0.2 rounded-full">
              0
            </span>
          </Link>

          {/* cart */}
          <Link to="/cart" className="relative hidden lg:flex">
            <FiShoppingBag className="text-[20px] text-secondary hover:text-primary/85" />
            <span className="absolute -top-1.5 -right-1.5 bg-red-400 text-white text-[10px] font-medium px-1 py-0.2 rounded-full">
              0
            </span>
          </Link>

          {/* Auth Links */}
          <div className="flex items-center">
            <Link
              to="/login"
              className="text-secondary  hover:text-primary/85 px-4 pt-1 pb-2 rounded-md"
            >
              Signin
            </Link>
          </div>
        </div>
      </div>
    </nav>
  );
};

export default Navbar;
```

## Mobile Bottom Navigation

`MobileBottomNav.jsx`

```js
//Package file import
import { useState } from "react";
import { Link } from "react-router-dom";

//React icons
import { FiMenu, FiShoppingBag, FiHome, FiHeart, FiGrid } from "react-icons/fi";

//Local file import
import CategoryMobileNav from "./CategoryMobileNav";
import MobileMenu from "./MobileMenu";

const MobileBottomNav = () => {
  const [menuOpen, setMenuOpen] = useState(false);
  const [categoryOpen, setCategoryOpen] = useState(false);

  return (
    <div>
      <div
        className="fixed bottom-0 left-1/2 transform -translate-x-1/2 w-full max-w-[500px] bg-white shadow-2xl 
      flex justify-around items-center py-2 z-30 lg:hidden"
      >
        {/* Menu Button */}
        <button
          onClick={() => setMenuOpen(true)}
          className="relative text-2xl text-text p-2 cursor-pointer  hover:text-primary"
        >
          <FiMenu />
        </button>

        {/* Home Button */}
        <Link
          to="/"
          className="relative text-2xl text-text p-2 cursor-pointer  hover:text-primary"
        >
          <FiHome />
        </Link>

        {/* Wishlist Button */}
        <button className="relative text-2xl text-text p-2 cursor-pointer  hover:text-primary">
          <FiHeart />
          <span className="absolute top-0 right-0 bg-red-400 text-white text-xs font-medium px-1.5 py-0.5 rounded-full">
            0
          </span>
        </button>

        {/* Shopping Bag Button */}
        <button className="relative text-2xl text-text p-2 cursor-pointer  hover:text-primary">
          <FiShoppingBag />
          <span className="absolute top-0 right-0 bg-red-400 text-white text-xs font-medium px-1.5 py-0.5 rounded-full">
            0
          </span>
        </button>

        {/* Grid/Menu Button */}
        <button
          onClick={() => setCategoryOpen(true)}
          className="relative text-2xl text-text p-2 cursor-pointer  hover:text-primary"
        >
          <FiGrid />
        </button>
      </div>
      <MobileMenu isOpen={menuOpen} onClose={() => setMenuOpen(false)} />
      <CategoryMobileNav
        isOpen={categoryOpen}
        onClose={() => setCategoryOpen(false)}
      />
    </div>
  );
};

export default MobileBottomNav;
```

## Mobile Navigation

`MobileMenu.jsx`

```js
//package import
// import React, { useState } from "react";
import { Link } from "react-router-dom";
// import { useSelector } from "react-redux";

//react-icons
import { FiX, FiChevronLeft } from "react-icons/fi";
import { navigationMenu } from "../../utils/data";

const MobileMenu = ({ isOpen, onClose }) => {
  //   const scrollToTop = () => {
  //     window.scrollTo({ top: 0, behavior: "smooth" });
  //   };

  return (
    <div>
      <div
        onClick={onClose}
        className={`fixed inset-0 bg-black/50 transition-opacity duration-300 z-40 lg:hidden ${
          isOpen ? "opacity-100 visible" : "opacity-0 invisible"
        }`}
      ></div>
      <nav
        className={`has-scrollbar fixed lg:hidden top-0 left-0 w-[320px] h-screen bg-white shadow-lg p-5 overflow-y-auto z-50 transition-transform duration-500 ${
          isOpen ? "translate-x-0" : "-translate-x-full"
        }`}
      >
        {/* Top Section */}
        <div className="flex justify-between items-center border-b border-gray-200 pl-2 pb-4 mb-4">
          <h2 className="text-lg font-semibold text-secondary">MENU</h2>
          <button
            onClick={onClose}
            className="text-text text-xl cursor-pointer hover:text-primary transition"
          >
            <FiX />
          </button>
        </div>

        <ul className="flex flex-col   font-medium text-gray-700">
          {navigationMenu?.map((nav, index) => (
            <li className="text-[13px] text-secondary hover:text-primary/85  px-2  py-2 hover:bg-gray-100 transition cursor-pointer">
              <Link onClick={onClose} key={index} to={nav.url}>
                {nav.name}
              </Link>
            </li>
          ))}
        </ul>

        <button className="w-full px-7 pb-2 pt-1 ml-2 mt-5 text-[13px] rounded cursor-pointer bg-primary text-white hover:bg-primary/80">
          Become Seller
        </button>
      </nav>
    </div>
  );
};

export default MobileMenu;
```

## Desktop Category Navigation

`CategoryDropdown.jsx`

```js
import {
  useState,
  useRef,
  useEffect,
  useLayoutEffect,
  useCallback,
} from "react";
import { FaChevronDown, FaChevronRight } from "react-icons/fa";
import { Link } from "react-router-dom";
import { categories } from "../../utils/data";

const CategoryDropdown = () => {
  const [isOpen, setIsOpen] = useState(false);
  const [hoveredIndex, setHoveredIndex] = useState(null);
  const dropdownRef = useRef(null);

  // close on outside click
  useEffect(() => {
    const handleClickOutside = (e) => {
      if (dropdownRef.current && !dropdownRef.current.contains(e.target)) {
        setIsOpen(false);
        setHoveredIndex(null);
      }
    };
    document.addEventListener("mousedown", handleClickOutside);
    return () => document.removeEventListener("mousedown", handleClickOutside);
  }, []);

  return (
    <div className="relative" ref={dropdownRef}>
      {/* Button */}
      <button
        onClick={() => setIsOpen((s) => !s)}
        className="flex items-center gap-1 text-[12px] border border-secondary  text-secondary px-2 py-1 rounded-md cursor-pointer"
      >
        Categories <FaChevronDown size={10} className="text-sm mt-1" />
      </button>

      {/* Main Dropdown */}
      {isOpen && (
        <div className="absolute left-0 mt-3 pb-4 w-[300px]  bg-white shadow-[0px_4px_6px_0px_rgba(0,_0,_0,_0.1)] z-50">
          <ul className="flex flex-col text-sm relative">
            {categories?.map((category, index) => (
              <CategoryItem
                key={index}
                category={category}
                isHovered={hoveredIndex === index}
                onEnter={() => setHoveredIndex(index)}
                onLeave={() => setHoveredIndex(null)}
                onSelect={() => setIsOpen(false)}
              />
            ))}
          </ul>
        </div>
      )}
    </div>
  );
};

const CategoryItem = ({ category, isHovered, onEnter, onLeave, onSelect }) => {
  const itemRef = useRef(null);
  return (
    <li
      ref={itemRef}
      onMouseEnter={onEnter}
      onMouseLeave={onLeave}
      className="group flex items-center  justify-between border-b-[1px] border-border-light px-4 py-2
       hover:bg-gray-100 transition cursor-pointer relative"
    >
      <div className="flex items-center gap-4">
        <img
          src={category.image}
          alt={category.name}
          className="rounded-full w-7 h-7"
        />
        {category.name}
      </div>
      <FaChevronRight className="text-xs text-gray-400 group-hover:text-gray-700" />

      {isHovered && category?.items?.length > 0 && (
        <SmartSubmenu anchorRef={itemRef}>
          {category.items.map((sub, i) => (
            <li
              key={i}
              className="px-4 py-2 hover:bg-gray-100 transition border-b-[1px] border-border-light"
            >
              <Link
                onClick={onSelect}
                to={`/category${category.url
                  .toLowerCase()
                  .replace(/ /g, "-")}/${sub.toLowerCase().replace(/ /g, "-")}`}
              >
                {sub}
              </Link>
            </li>
          ))}
        </SmartSubmenu>
      )}
    </li>
  );
};

/**
 * SmartSubmenu
 * - Positions relative to the anchor <li>
 * - Prevents viewport overflow vertically (clamps top & bottom)
 * - Flips left when there isn't enough space on the right
 * - Adds max height with scroll
 */
const SmartSubmenu = ({ anchorRef, children }) => {
  const submenuRef = useRef(null);
  const [style, setStyle] = useState({}); // { top, left/right }
  const MARGIN = 8;
  const MAX_VH = 0.72; // 72vh cap for submenu

  const computePosition = useCallback(() => {
    const anchor = anchorRef.current;
    const menu = submenuRef.current;
    if (!anchor || !menu) return;

    // reset to measure natural size
    menu.style.maxHeight = "";
    menu.style.left = "100%";
    menu.style.right = "auto";
    menu.style.top = "0px";

    const anchorRect = anchor.getBoundingClientRect();

    // Ensure we know the menu’s natural height
    const maxHeightPx = Math.floor(window.innerHeight * MAX_VH);
    const naturalHeight = Math.min(menu.scrollHeight, maxHeightPx);

    // Horizontal placement: prefer right; flip left if not enough space
    const menuWidth = menu.offsetWidth || 224; // fallback if not rendered width yet
    const spaceRight = window.innerWidth - anchorRect.right;
    const spaceLeft = anchorRect.left;

    let openLeft = false;
    if (spaceRight < menuWidth + MARGIN && spaceLeft > spaceRight) {
      openLeft = true;
    }

    // Vertical placement: clamp inside viewport
    const spaceBelow = window.innerHeight - anchorRect.top; // from anchor top to bottom of viewport
    let topOffset = 0;

    if (spaceBelow < naturalHeight + MARGIN) {
      // shift upward so bottom fits
      topOffset = -(naturalHeight - spaceBelow + MARGIN);
      // but don't go above the viewport top
      if (anchorRect.top + topOffset < MARGIN) {
        topOffset = MARGIN - anchorRect.top;
      }
    }

    setStyle({
      top: `${topOffset}px`,
      ...(openLeft
        ? { right: "100%", left: "auto" }
        : { left: "100%", right: "auto" }),
      maxHeight: `${maxHeightPx}px`,
    });
  }, [anchorRef]);

  // Recompute on mount/hover, resize, and scroll
  useLayoutEffect(() => {
    computePosition();
    const onWinChange = () => computePosition();
    window.addEventListener("resize", onWinChange);
    window.addEventListener("scroll", onWinChange, true); // true => capture scroll from ancestors
    return () => {
      window.removeEventListener("resize", onWinChange);
      window.removeEventListener("scroll", onWinChange, true);
    };
  }, [computePosition]);

  return (
    <ul
      ref={submenuRef}
      className="absolute ml-1 w-56 bg-white shadow-[0px_4px_6px_0px_rgba(0,_0,_0,_0.1)] overflow-y-auto"
      style={style}
    >
      {children}
    </ul>
  );
};

export default CategoryDropdown;
```

## Mobile Category Navigation

`categoryMobileNav.jsx`

```js
//package file imports
import { useState } from "react";
import { Link } from "react-router-dom";

//React file icons
import { FiX, FiPlus, FiMinus } from "react-icons/fi";

//Local file imports
import { categories } from "../../utils/data";

const CategoryMobileNav = ({ isOpen, onClose }) => {
  const [openCategory, setOpenCategory] = useState(null);

  const toggleCategory = (index) => {
    setOpenCategory(openCategory === index ? null : index);
  };

  return (
    <div>
      <div
        onClick={onClose}
        className={`fixed inset-0 bg-black/50 transition-opacity duration-300 z-40 lg:hidden ${
          isOpen ? "opacity-100 visible" : "opacity-0 invisible"
        }`}
      ></div>
      <nav
        className={`has-scrollbar fixed lg:hidden top-0 left-0 w-[350px] h-screen bg-white shadow-lg p-5 overflow-y-auto z-50 transition-transform duration-500 ${
          isOpen ? "translate-x-0" : "-translate-x-full"
        }`}
      >
        {/* Top Section */}
        <div className="flex justify-between items-center border-b border-gray-200 pl-3  pb-4 mb-4">
          <h2 className="text-lg font-semibold text-secondary ">Menu</h2>
          <button
            onClick={onClose}
            className="text-gray-700 text-xl cursor-pointer hover:text-primary/80"
          >
            <FiX />
          </button>
        </div>

        {/* Categories Section */}
        <ul className="space-y-3 pb-4">
          {categories?.map((category, index) => (
            <li key={index}>
              <button
                onClick={() => toggleCategory(index)}
                className="w-full flex justify-between items-center text-[12px] text-gray-800 font-medium 
                cursor-pointer hover:text-primary/80 transition pb-1 "
              >
                <span className="flex items-center ml-3 ">
                  <img
                    className="w-8 h-8 rounded-full"
                    src={category.image}
                    alt=""
                  ></img>
                  <span className="pl-4 ">{category.name}</span>
                </span>
                {category.items.length > 0 ? (
                  openCategory === index ? (
                    <FiMinus />
                  ) : (
                    <FiPlus />
                  )
                ) : null}
              </button>

              {openCategory === index && category.items.length > 0 && (
                <ul className="pl-14 pt-2 space-y-2 text-xs transition-all duration-500 border-t border-gray-100 pb-1">
                  {category.items.map((item, subIndex) => (
                    <li
                      key={subIndex}
                      className="px-4 py-2 block hover:bg-gray-100 transition hover:text-primary cursor-pointer "
                    >
                      <Link
                        onClick={onClose}
                        to={`/category${category.url
                          .toLowerCase()
                          .replace(/ /g, "-")}/${item
                          .toLowerCase()
                          .replace(/ /g, "-")}`}
                      >
                        {item}
                      </Link>
                    </li>
                  ))}
                </ul>
              )}
            </li>
          ))}
        </ul>
      </nav>
    </div>
  );
};

export default CategoryMobileNav;
```
